# Styling to apply to the app

Colours can be either hex strings or names, but if using names they must
be recognized both by R and as CSS colour names.

## Usage

``` r
ov_app_styling()
```

## Value

A named list of styling parameters

## Details

- `review_pane_width` is expressed as a percentage of the browser window
  width
